---
phase: 05-matching-core
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/app/dashboard/queue/page.tsx
  - src/app/dashboard/queue/role-card.tsx
  - src/app/dashboard/roles/[jobId]/brief/page.tsx
  - src/app/dashboard/roles/[jobId]/brief/requirement-row.tsx
  - src/app/dashboard/roles/[jobId]/brief/mapping-controls.tsx
  - src/app/dashboard/page.tsx
autonomous: false

must_haves:
  truths:
    - "User can view Fresh Match Queue page with ranked role cards"
    - "Each role card shows fit band (High/Medium/Low), top reasons, and next action — NOT percentages"
    - "User can open role brief showing requirement-to-evidence mapping with gaps"
    - "Role brief displays covered requirements with evidence excerpts and gap requirements"
    - "User can manually edit or remove evidence mappings from the role brief"
    - "Matching state indicators visible: 'Run Matching' button, loading spinner, stale badge, needs review badge"
    - "Changes to mappings update the UI via TanStack Query cache invalidation"
  artifacts:
    - path: "src/app/dashboard/queue/page.tsx"
      provides: "Fresh Match Queue page with ranked role cards"
      contains: "useMatchQueue"
    - path: "src/app/dashboard/queue/role-card.tsx"
      provides: "Individual role card component showing fit band and reasons"
      contains: "fitBand"
    - path: "src/app/dashboard/roles/[jobId]/brief/page.tsx"
      provides: "Role Brief page with proof mapping and gaps"
      contains: "useRoleBrief"
    - path: "src/app/dashboard/roles/[jobId]/brief/requirement-row.tsx"
      provides: "Individual requirement row with evidence mapping"
      contains: "RequirementRow"
    - path: "src/app/dashboard/roles/[jobId]/brief/mapping-controls.tsx"
      provides: "Controls for editing/removing mappings"
      contains: "MappingControls"
  key_links:
    - from: "src/app/dashboard/queue/page.tsx"
      to: "/api/matching/queue"
      via: "useMatchQueue hook"
      pattern: "useMatchQueue"
    - from: "src/app/dashboard/queue/role-card.tsx"
      to: "src/app/dashboard/roles/[jobId]/brief/page.tsx"
      via: "Link navigation"
      pattern: "Link.*roles.*brief"
    - from: "src/app/dashboard/roles/[jobId]/brief/page.tsx"
      to: "/api/matching/[jobId]/brief"
      via: "useRoleBrief hook"
      pattern: "useRoleBrief"
    - from: "src/app/dashboard/roles/[jobId]/brief/mapping-controls.tsx"
      to: "/api/matching/[jobId]/mappings"
      via: "useUpdateMapping and useDeleteMapping hooks"
      pattern: "useUpdateMapping|useDeleteMapping"
---

<objective>
Build the Fresh Match Queue page with ranked role cards and the Role Brief page with requirement-to-evidence proof mapping, gap display, and manual mapping controls.

Purpose: Delivers the user-facing product surface for Phase 5 — the ranked queue where students see their best-fit opportunities and the role brief where they see exactly how their evidence maps to each requirement.

Output: Two new pages (queue + brief) accessible from dashboard navigation.
</objective>

<execution_context>
@C:\Users\Mekyle\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Mekyle\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-matching-core/05-CONTEXT.md
@.planning/phases/05-matching-core/05-03-SUMMARY.md
@src/lib/hooks/use-match-queue.ts
@src/lib/hooks/use-role-brief.ts
@src/lib/hooks/query-keys.ts
@src/app/dashboard/page.tsx
@src/app/dashboard/jobs/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Fresh Match Queue page with role cards</name>
  <files>src/app/dashboard/queue/page.tsx, src/app/dashboard/queue/role-card.tsx, src/app/dashboard/page.tsx</files>
  <action>
**1. Create `src/app/dashboard/queue/page.tsx` — Fresh Match Queue page:**

This is the primary product page where users see their ranked opportunities.

Implementation:
- 'use client' component using useMatchQueue hook
- Page title: "Fresh Match Queue"
- Subtitle: "Ranked by fit and freshness"
- Loading state: skeleton cards
- Empty state: "No matches yet. Set up your criteria and upload evidence to get started." with links to /dashboard/criteria and /dashboard/evidence
- Error state: retry button with error message
- Map over queue data, render RoleCard for each job
- Include "Run Matching" button (uses useRunMatching hook) for manual trigger per CONTEXT.md locked decision #7

**2. Create `src/app/dashboard/queue/role-card.tsx` — Individual role card:**

Per CONTEXT.md locked decision #4: Show fit bands (High/Medium/Low) with reasons, NOT percentages.
Per CONTEXT.md locked decision #12 (CORE-12): Each role card shows fit score, freshness indicator, evidence coverage %, and next action.

**IMPORTANT RECONCILIATION:** CONTEXT.md says "fit bands not percentages" but CORE-12 says "evidence coverage %". Resolution: Show fit band prominently (High/Medium/Low) with color coding. Show coverage as "X of Y requirements covered" (count, not percentage). Show next action. Show freshness indicator.

Card layout:
- Left: Title (bold), Company, Location
- Right: Fit band badge with color (High=green, Medium=yellow, Low=red)
- Below title: Top 2-3 fit reasons as text items
- Footer: "X of Y requirements covered" | freshness indicator ("Posted X days ago") | next action button/text
- Matching state indicator per CONTEXT.md locked decision #7:
  - Not matched: Show "Run Matching" button
  - In progress: Loading spinner
  - Stale: "Re-run to refresh" badge
  - Needs review: Review count badge (e.g., "3 need review")
  - Up to date: no indicator
- Click anywhere on card navigates to /dashboard/roles/[jobId]/brief

Tailwind styling:
- Card: `border rounded-lg p-4 hover:bg-gray-50 cursor-pointer`
- Fit band badge:
  - High: `bg-green-100 text-green-800`
  - Medium: `bg-yellow-100 text-yellow-800`
  - Low: `bg-red-100 text-red-800`
- Reasons: `text-sm text-gray-600`
- Next action: `text-blue-600 font-medium`

**3. Update `src/app/dashboard/page.tsx`:**

Add a navigation link or card for "Fresh Match Queue" that links to /dashboard/queue. Look at the existing dashboard page structure first and add consistently with existing dashboard navigation items.
  </action>
  <verify>
- `npm run build` passes with no TypeScript errors
- `src/app/dashboard/queue/page.tsx` uses useMatchQueue hook
- `src/app/dashboard/queue/role-card.tsx` shows fitBand (High/Medium/Low), NOT percentage
- Role card shows "X of Y requirements covered" format
- Role card shows matching state indicators per CONTEXT.md locked decision #7
- Card links to /dashboard/roles/[jobId]/brief
- Dashboard page has navigation to /dashboard/queue
  </verify>
  <done>
- Fresh Match Queue page renders ranked role cards
- Fit bands displayed as High/Medium/Low with color coding, not percentages
- Role cards show fit reasons, requirement coverage count, freshness, and next action
- Matching state indicators visible (run matching, loading, stale, needs review)
- Queue page accessible from dashboard navigation
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Role Brief page with proof mapping and mapping controls</name>
  <files>src/app/dashboard/roles/[jobId]/brief/page.tsx, src/app/dashboard/roles/[jobId]/brief/requirement-row.tsx, src/app/dashboard/roles/[jobId]/brief/mapping-controls.tsx</files>
  <action>
**1. Create `src/app/dashboard/roles/[jobId]/brief/page.tsx` — Role Brief page:**

This is the proof mapping page where users see exactly how their evidence maps to each requirement.

Implementation:
- 'use client' component using useRoleBrief(jobId) hook
- Extract jobId from route params
- Loading/error states

Page sections:
a) **Header:** Job title, company, fit band badge (large), "Run Matching" or "Re-run" button based on matchingState
b) **Fit Summary Card:**
   - Fit band (High/Medium/Low) large and prominent
   - Top fit reasons listed (2-3 reasons from API)
   - Requirements summary: "X covered, Y gaps, Z need review"
c) **Recommended Emphasis** (if available): "Highlight these in your application:" followed by top 3 evidence items
d) **Covered Requirements Section:** List of requirements that have evidence mappings
   - Each rendered as RequirementRow component
   - Grouped by category (technical, experience, education, soft_skills, etc.)
e) **Gaps Section:** List of requirements with NO evidence
   - Sorted by priority (required first)
   - Styled with warning indicators
   - Each shows requirement text and category/priority
f) **Back link** to /dashboard/queue

**2. Create `src/app/dashboard/roles/[jobId]/brief/requirement-row.tsx`:**

Displays a single requirement with its evidence mappings.

Props: requirement data with mapped evidence array

Layout:
- Requirement text (from normalizedText)
- Category and priority badges
- For each evidence mapping:
  - Evidence title
  - Supporting excerpt (sourceEvidenceExcerpt from provenance)
  - Decision badge: match (green), weak_match (yellow)
  - Confidence band badge
  - needsReview indicator if flagged
  - MappingControls component for edit/delete
- If no mappings (gap): Show "No evidence mapped" with different styling

**3. Create `src/app/dashboard/roles/[jobId]/brief/mapping-controls.tsx`:**

Controls for editing and removing evidence mappings (per CORE-08).

Implementation:
- "Remove" button — Uses useDeleteMapping mutation, shows confirmation
- "Edit" button — Expands inline edit form with:
  - Reason text area
  - Decision dropdown (match/weak_match)
  - Manual override reason (required field)
  - Save/Cancel buttons
- Uses useUpdateMapping mutation
- On success: cache invalidation via TanStack Query (brief and queue refresh)

Per CONTEXT.md locked decision #7: Manual edits do NOT trigger re-matching.
Per CONTEXT.md locked decision #8: Store manualOverrideReason for all user edits.

Tailwind styling:
- Remove button: `text-red-600 text-sm hover:underline`
- Edit form: `border rounded p-3 mt-2 bg-gray-50`
- Save: `bg-blue-600 text-white px-3 py-1 rounded`
- Cancel: `text-gray-600 text-sm`
  </action>
  <verify>
- `npm run build` passes with no TypeScript errors
- `src/app/dashboard/roles/[jobId]/brief/page.tsx` uses useRoleBrief hook
- Brief page shows fit band (High/Medium/Low), NOT percentage
- Brief page has separate sections for covered requirements and gaps
- Requirement rows show evidence excerpts (provenance) and decision badges
- Mapping controls allow edit and delete with manualOverrideReason
- Cache invalidation occurs on mapping mutations (grep for invalidateQueries)
  </verify>
  <done>
- Role Brief page renders proof mapping with requirement-to-evidence links
- Covered requirements show evidence excerpts and confidence badges
- Gap requirements highlighted with priority ordering
- Recommended emphasis section shows top evidence items
- Manual edit controls allow changing decision, reason, and removing mappings
- All edits store manualOverrideReason in audit trail
- UI updates via TanStack Query cache invalidation
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Fresh Match Queue page with ranked role cards showing fit bands, and Role Brief page with requirement-to-evidence proof mapping, gap display, and manual mapping controls.
  </what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Navigate to http://localhost:3000/dashboard
3. Verify "Fresh Match Queue" link/card is visible
4. Click through to /dashboard/queue
5. Verify queue page shows ranked role cards with:
   - Fit band badges (High/Medium/Low) — NOT percentages
   - Top reasons for each role
   - Requirement coverage as "X of Y requirements covered"
   - Freshness indicator
   - Next action text
   - Matching state indicators (Run Matching button if no matches exist)
6. Click a role card to navigate to role brief
7. Verify role brief page shows:
   - Fit summary with fit band
   - Covered requirements with evidence excerpts
   - Gaps section with unfilled requirements
   - Recommended emphasis
8. Test manual mapping controls:
   - Click "Edit" on a mapping, verify inline form appears
   - Verify manualOverrideReason is required
   - Click "Remove" on a mapping, verify confirmation
9. Verify UI updates after mutations (no full page reload needed)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Fresh Match Queue page renders at /dashboard/queue
- Role Brief page renders at /dashboard/roles/[jobId]/brief
- Fit bands shown as High/Medium/Low (not percentages) per CONTEXT.md locked decision #4
- Matching state indicators per CONTEXT.md locked decision #7
- Manual mapping edit/delete works with audit trail per CONTEXT.md locked decision #8
- Evidence excerpts shown in requirement rows (provenance) per CONTEXT.md locked decision #8
- UI updates via TanStack Query invalidation, no full page reload
- `npm run build` passes
</verification>

<success_criteria>
- User sees ranked opportunities in queue with fit bands and reasons
- User can drill into role brief showing requirement-by-requirement proof mapping
- User can see gaps (requirements with no evidence)
- User can manually edit/remove mappings
- All interactions are real-time via TanStack Query
- UI matches CONTEXT.md decisions: bands not percentages, provenance shown, matching states visible
</success_criteria>

<output>
After completion, create `.planning/phases/05-matching-core/05-04-SUMMARY.md`
</output>
